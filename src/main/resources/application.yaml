spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    format-sql: true

  application:
    name: prj
  datasource:
    url: jdbc:mariadb://localhost:3306
    username:
    password:

